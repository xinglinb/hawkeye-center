import React from 'react';
import './index.scss';

export default({ className }) => (
  <div className={`${className} logo-box`}>
    <i className="iconfont logo-left">&#xe61d;</i>
    <span className="logo-font">HAWK EYE</span>
  </div>
);

